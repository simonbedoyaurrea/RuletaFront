import React from 'react'
import '../styles/titulo.css'

export default function Titulo() {
  return (
    <div className='contenedor-titulo'>
        <h1 className='titulo-principal'>The Redemption Roulette</h1>
        {/* <button  onClick={() => navigator.clipboard.writeText(roomId)}>codigo de la sala: {roomId}</button> */}
    </div>
  )
}
